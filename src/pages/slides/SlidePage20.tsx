import SlideLayout from "@/components/presentation/SlideLayout";
import SlideBrands from "@/components/presentation/slides/SlideBrands";

const SlidePage20 = () => {
  return (
    <SlideLayout currentSlide={20} totalSlides={20}>
      <SlideBrands />
    </SlideLayout>
  );
};

export default SlidePage20;
